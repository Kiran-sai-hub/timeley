PORT=5001
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://localhost:27017/timeley

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS — must be a specific origin (not *) for httpOnly cookies to work
CORS_ORIGIN=http://localhost:5173
